<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Just To-Do It!</title>
        <link href="/css/style.css" rel="stylesheet" type="text/css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    </head>
    <body>
        <h1>Why do something when you can just To-Do it?</h1>
        <div id="container">
            <ul class="items">
                <hr><h3>To-Do:</h3>
                <%if(items.length == 0){%>
                    <p style="color:#555">Your list is empty.</p>
                <%}%>
                <%for(var i=0; i<items.length; i++) {%>
                    <li class="item">
                        <input onchange="selected('<%= items[i].item %>')" type="checkbox">
                        <span><%= items[i].item %></span>
                        <a onclick="deleteItem('<%= items[i].item %>')" class="glyphicon glyphicon-trash"></a>
                        <hr>
                    </li>
                <%}%>
                <form action="/addItem" method="POST">
                    <input id="cookieField" hidden="true" type="text" name="cookie" value="">
                    <input id="_done" hidden="true" type="text" name="done" value="false">
                    <input id="_deleted" hidden="true" type="text" name="deleted" value="false">
                    <input  autofocus="true" type="text" placeholder="[Enter To-Do]" name="item">
                        <button type="submit">To do!</button>
                </form>
                <hr>
            </ul>



            <ul class="items_done">
                <hr><h3>Done:</h3>
                <%if(items_done.length == 0){%>
                    <p style="color:#555">Nothing done yet.</p>
                <%}%>
                <%for(var i=0; i<items_done.length; i++) {%>
                    <li class="item_done">
                        <span style='text-decoration: line-through'><%= items_done[i].item %></span>
                        <a onclick="deleteItem('<%= items_done[i].item %>')" class="glyphicon glyphicon-trash"></a>
                        <hr>
                    </li>
                <%}%>
            </ul>
        </div>
        <script src="js/main.js"></script>
    </body>
</html>
